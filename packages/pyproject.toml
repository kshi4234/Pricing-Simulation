[project]
name = "packages"
version = "0.1.0"
description = ""
authors = [
    {name = "kshi4234",email = "kshi4234@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[tool.poetry]
packages = [{include = "packages", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
